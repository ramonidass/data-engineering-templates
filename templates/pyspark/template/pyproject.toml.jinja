[project]
name = "{{ project_name }}"
version = "0.1.0"
description = "{{ product_name_display }} PySpark jobs"
readme = "README.md"
requires-python = ">={{ python_version }}"
dependencies = [
    "pyspark>={{ spark_version }},<{{ '3.6' if spark_version == '3.5' else '4.3' }}",
    "pydantic>=2.13,<3",
    "pydantic-settings>=2.7",
]

[dependency-groups]
dev = [
    "pytest>=8.3",
    "pytest-cov>=6.0",
    "ruff>=0.16,<1",
    "mypy>=1.14",
    "pre-commit>=4.0",
]

[project.scripts]
daily-aggregation = "{{ package_name }}.jobs.daily_aggregation:main"
streaming-aggregation = "{{ package_name }}.jobs.streaming_aggregation:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/{{ package_name }}"]

[tool.ruff]
line-length = {{ python_line_length }}
target-version = "py{{ python_version | replace('.', '') }}"
extend-exclude = [".venv", ".direnv"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "SIM", "PTH", "RUF"]

[tool.mypy]
strict = true
exclude = ["\\.venv", "\\.direnv"]

[[tool.mypy.overrides]]
module = "pyspark.*"
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
